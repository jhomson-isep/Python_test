<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <!-- Inherit search View to Modify it -->
        <record id="view_purchase_order_filter_inherit" model="ir.ui.view">
            <field name="name">view.purchase.order.filter.inherit</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.view_purchase_order_filter"/>
            <field name="arch" type="xml">
                <xpath expr="//filter[@name='my_purchases']" position="after">
                    <filter name="has_invoice_attach" string="Has invoice"
                            domain="[('has_invoice_attached','=', True)]"/>
                    <separator/>
                </xpath>
            </field>
        </record>

        <template id="portal_purchase_signature" name="Ask Signature">
            <div t-att-class="'o_purchase_signature_form'"
                 t-att-data-partner-name="partner_name"
                 t-att-data-res-model="object._name"
                 t-att-data-res-id="object.id"
                 t-att-data-call-url="callUrl"
                 t-att-data-sign-label="signLabel"
                 t-att-data-access-token="accessToken"/>
        </template>

    </data>
</odoo>
